// One file per basemap. Add new basemaps here — one import, one named export.
import { basemapNasaBlueMarble } from "./nasa-blue-marble";

export const basemaps = [basemapNasaBlueMarble];

export { basemapNasaBlueMarble };
